import { getReports } from "../inputs.ts";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//
//	
// 
// 
// 
// 
//
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

const formattedReports = async () => {
	const reps = (await getReports).split("\n").map(rep => rep.split(" "))
	const rmLast = reps.slice(0, reps.length - 1)
	return rmLast
}

export { formattedReports }
